class MenuItem {
    constructor(title, link, icon, submenu) {
      this.title = title;
      this.link = link;
      this.icon = icon;
      this.submenu = submenu;
    }
  
  }

  
  export default MenuItem;